%层次分析法判别矩阵的一致性检验代码
clc,clear
P=[1.0000 3.0000 4.0000;
0.3333 1.0000 2.0000;
0.2500 0.5000 1.0000];%判别矩阵 
[L,G]=eig(P);%求特征根和相应的特征向量 
w=L(:,1)/sum(L(:,1));%归一化特征向量近似为权重值 
max=max(eig(P));%求最大特征根
n=size(P); CI=(max-3)/(3-1);
RI=1.12; %查表对应 n=3 的情况 CR=CI/RI
if CR>=0.1
	error('A 不通过一致性检验') 
end